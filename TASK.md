# 完整首轮任务

请从零实现“带因果上下文的可合并集合内核”，用于分布式算法 / CRDT中的本地可复用模块。

1. 从零实现状态式 observed-remove set，元素为字符串，每次 Add 生成 dot=(replicaID,递增计数)。仅在内存中模拟多个固定唯一副本，不联网，不需要远程节点。
2. 状态由当前活跃的 element->dots 和已观察 dot 的因果上下文组成；Remove 只清除本副本观察到的该元素 dots，未观察到的并发 Add 应保留。上下文用每副本连续前缀+离散异常点压缩，不能用单个最大计数吞掉空洞。
3. Merge 保留双方共有活跃 dot，以及只在一方活跃且尚未被另一方上下文观察到的 dot；合并上下文取并集。删除后的状态经延迟旧状态合并不能复活已删除 dot；不提供未经证明安全的墓碑清理。
4. 提供确定性快照编码和恢复，验证活跃 dots 必须被上下文包含、同一 dot 不能对应不同元素、计数和副本 ID 合法；坏快照或冲突状态合并整体拒绝，禁止部分修改。恢复后的本副本计数不复用已有 dot。
5. 用未压缩 dot 集合模型作参考，验证交换律、结合律、幂等性，以及固定种子断连/重连/乱序/重复状态传播。限制20副本、每副本10000次 Add，无需后台任务或真实时钟。

验收重点：并发增删、观察后删除、删除先到与旧快照后到、上下文空洞、相同字符串多dot、序列化往返、计数冲突。

工程范围：这是全新核心库的0-1实现，初始仓库仅有运行骨架，没有业务实现。仅使用Go 1.26.5标准库，在Windows原生环境离线运行；无需安装第三方依赖。不接数据库、中间件、云API、外部程序，不使用Docker，不制作前后端或通用CLI。
交付：实现可复用库、聚焦上述语义的自动化测试和README；go run ./cmd/demo须在约8秒内运行确定性小案例，打印关键正常结果和至少一种真实边界失败，便于录制十几秒视频。测试命令为go test ./... -count=1 -timeout=60s。先阅读本仓库README和TASK.md，简短计划后集中实现，再运行针对性测试与演示；避免反复通读仓库、无关探索和无必要的长篇文档，发现实际问题时正常修复验证，不设硬性工具调用截断。
只能操作当前仓库内文件；不读取用户配置、密钥、环境变量清单或其他工作区。不要新增GitHub Actions、CI、部署、Dependabot或远程推送。一次首轮完成，不要求用户追加“继续”；合理实现取舍写入README。不要为通过测试删除需求、硬编码示例输出或宣称未实际执行的验证通过。
