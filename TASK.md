# 完整首轮任务

需要一个能在离线副本之间合并的集合库，支持添加、删除和快照恢复。请实现带因果上下文的 observed-remove set，按下面的规则处理并发操作和副本身份。

实现状态式observed-remove set，元素为字符串，Add生成dot=(replicaID,计数)。固定唯一副本ID各只有一个写者；调用者不得用同ID并发运行或从旧快照回退写者。只在内存模拟，不联网。

状态含element->活跃dots和已观察因果上下文。Remove仅移除已观察的该元素dots，并发未见Add保留。上下文用每副本连续前缀+离散异常点，空洞不能被最大计数吞掉。

Merge保留双方共有活跃dot，以及单侧活跃且未被另一侧上下文观察的dot；上下文取并。被删除dot不会因旧状态重传复活，不额外做墓碑回收或动态副本成员管理。

JSON快照按数组排序确定编码，校验ID、正计数、活跃dot属于上下文及当前可见同dot不能对应不同元素。无法检测已删除dot的所有历史伪造，不作该保证。合法恢复计数取本副本上下文最大值（含异常点），计数不回绕；错误整体拒绝。

最多20副本，每副本计数<=10000。用未压缩dot集合参考核对压缩上下文与合并的交换/结合/幂等律，构造并发增删、空洞、乱序重复快照；不改写传入快照。

测试里请覆盖这些情况：并发Add与Remove、观察后删除、旧状态重传、上下文空洞、异常点高计数恢复、可见dot冲突、输入不变。

使用 Go 1.26.5 标准库，代码要能在 Windows 原生环境离线运行。范围就是这个库，不需要第三方依赖、外部服务、Docker 或前后端。请一并写好测试和简短的 README，说明接口及运行方法。

用 `go run ./cmd/demo` 运行演示，在约 8 秒内展示一个正常结果和一个实际触发的失败；用 `go test ./... -count=1 -timeout=60s` 运行测试。随机验证采用固定种子和少量小样本即可，不做长时间压力测试，也不扩展额外的平台功能。

请在本轮完成实现和验证，不要等我回复“继续”。正常排错需要的工具调用不设次数限制。操作范围限于当前仓库，不读取用户配置、密钥、环境变量清单或其他工作区。不要添加 Actions、CI、部署或 Dependabot，也不要向远端推送。演示结果应由代码实际计算；最后如实说明验证结果，未运行或未通过的检查也请写明。
