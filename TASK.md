# 完整首轮任务

断连副本在本地维护资源标签集合，交换内存快照后应收敛且不复活已观察删除。请从零实现“带因果上下文的可合并集合内核”。

1. 实现状态式observed-remove set，元素为字符串，Add生成dot=(replicaID,计数)。固定唯一副本ID各只有一个写者；调用者不得用同ID并发运行或从旧快照回退写者。只在内存模拟，不联网。
2. 状态含element->活跃dots和已观察因果上下文。Remove仅移除已观察的该元素dots，并发未见Add保留。上下文用每副本连续前缀+离散异常点，空洞不能被最大计数吞掉。
3. Merge保留双方共有活跃dot，以及单侧活跃且未被另一侧上下文观察的dot；上下文取并。被删除dot不会因旧状态重传复活，不额外做墓碑回收或动态副本成员管理。
4. JSON快照按数组排序确定编码，校验ID、正计数、活跃dot属于上下文及当前可见同dot不能对应不同元素。无法检测已删除dot的所有历史伪造，不作该保证。合法恢复计数取本副本上下文最大值（含异常点），计数不回绕；错误整体拒绝。
5. 最多20副本，每副本计数<=10000。用未压缩dot集合参考核对压缩上下文与合并的交换/结合/幂等律，构造并发增删、空洞、乱序重复快照；不改写传入快照。

验收重点：并发Add与Remove、观察后删除、旧状态重传、上下文空洞、异常点高计数恢复、可见dot冲突、输入不变。

工程约束：全新0-1核心库，仅用Go 1.26.5标准库，在Windows原生离线运行，无第三方依赖、外部服务、Docker或前后端。交付实现、聚焦上述边界的测试及简短README；演示命令`go run ./cmd/demo`用小案例在约8秒内打印正常结果和一种真实失败，测试命令`go test ./... -count=1 -timeout=60s`。固定种子随机验证只用少量小样本，避免长压测和额外平台功能，不限制正常纠错所需工具调用。
一次首轮完成，不要求用户追加“继续”。只操作当前仓库，不读取用户配置、密钥、环境变量清单或其他工作区；不添加Actions/CI/部署/Dependabot、不远程推送，不硬编码演示或虚报验证。
